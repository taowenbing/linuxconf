#! /bin/sh

# /etc/init.d/boot.local <-- rc-local.service
# /etc/init.d/after.local <-- after-local.service

# for tty in /dev/tty[1-6]; do
#     setleds -D +num < $tty
# done
# unset tty

# modprobe gamecon map=0,7
